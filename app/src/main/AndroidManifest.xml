<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiz">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Teacher.TeacherResultActivity"></activity>
        <activity android:name=".Student.StudentResultActivity" />

        <service
            android:name=".Student.SaatServis"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Student.StudentTestActivity" />
        <activity android:name=".Teacher.TeacherTestActivity" />
        <activity android:name=".SelectPage" />
        <activity android:name=".Teacher.TeacherActivity" />
        <activity android:name=".Student.StudentActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>